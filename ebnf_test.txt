character = ? character ?;

binary digit = '1'
       	     | '0';

(* you're such and idiot *)

binary literal = ['-'],binary digit,{binary digit};

comment = '/*',{character},'*/'
	| '//',{character - '\n'},'\n';
